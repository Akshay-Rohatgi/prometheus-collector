{{- if and .Values.namespace.create (ne .Release.Namespace .Values.namespace.name) }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.name }}
  labels:
    {{- include "llm.labels" . | nindent 4 }}
{{- end }}
