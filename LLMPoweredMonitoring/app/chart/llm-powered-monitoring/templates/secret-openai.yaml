{{- if and .Values.secrets.openai.create .Values.secrets.openai.data }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.openai.name }}
  namespace: {{ .Values.namespace.name | default .Release.Namespace }}
  labels:
    {{- include "llm.labels" . | nindent 4 }}
type: Opaque
stringData:
  .env: |
{{- range $k, $v := .Values.secrets.openai.data }}
    {{ $k }}={{ $v }}
{{- end }}
{{- end }}
